#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    
    cout << "Enter three side lengths: \n";
    cin >> a >> b >> c;
    
    if(a+b>c && b+c>a && a+c>b){
        cout << "This is a triangle";
    }else{
        cout << "This isn't a triangle";
    }
}
